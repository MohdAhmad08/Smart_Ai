# Copy to frontend/.env and edit.
#   cp .env.example .env
#
# Backend API base URL — must be an address reachable by whoever is VIEWING
# the dashboard, not the machine running it. "localhost" only works when the
# viewer and the server are the same machine.
VITE_API_URL=http://localhost:8000
